name: requests-ecp
channels:
  - conda-forge
dependencies:
  - python>=3
  # build
  - pip
  - setuptools
  # install
  - lxml
  - requests
  - requests-gssapi>=1.2.2
  # tests
  - requests-mock>=1.5.0
  # docs
  - furo
  - sphinx
  - sphinx-tabs
  - sphinx-automodapi !=0.17.0
